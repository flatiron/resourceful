
- Fix validator functions to throw TypeError instead of Error.
- Make value of 'resource' field lowercase accross the board.
- Retrieve objects directly from cache, if available.
- Turn the object cache into an LRU.
- Implement *file* backend.
- Implement *rest* backend.
- Make sure we bypass the 'n+1' problem.
